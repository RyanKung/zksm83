# Akita protocol schedule

`fp128_dense_bounded.aks` is a verifier-relevant protocol input copied without
modification from LayerZero-Labs/akita at revision
`69438de6cd8ce8ed7c9ebb21bdf60b813e8fcabc`.

- SHA-256: `c2098502e4c976a6a6cf687e4f70acfcb818372e2fbd589bff7b18e8decfa9cf`
- Upstream path: `artifacts/schedules/fp128_dense_bounded.aks`
- Config: `akita_config::proof_optimized::fp128::DenseBounded`
- Transcript: Blake2b

The artifact is committed because schedule admission is part of verification.
Runtime-generated proofs, traces, checkpoints, benchmark output, ROMs, save
files, and test data remain excluded by the repository `.gitignore`.

## ZKSM83 relation catalog

`fp128_dense_bounded_nv14_p128.aks` is generated by the same pinned Akita
planner with:

```text
AKITA_SCHEDULE_GEN_JOBS=1 cargo +1.95.0 run --release \
  -p akita-planner --features catalog-gen \
  --bin gen_schedule_artifacts -- <isolated-output-dir> --row-progress \
  --final-group fp128_dense_bounded:14:128
```

It admits exactly one final-only group with 14 variables and 128
polynomials. Its SHA-256 is
`e601bc0bd9d4501220c367b3012901aac09467f145899c8e907b282d30e96646`.
Native relations commit fixed 128-column groups and zero-pad the last group,
so proof bytes cannot select an unreviewed shape.

`fp128_dense_bounded_nv9_p128.aks` was generated by replacing the final
argument above with:

```text
--final-group fp128_dense_bounded:9:128
```

It admits exactly one final-only group with 9 variables and 128 polynomials
for the verifier-fixed 512-row ISA table. Its SHA-256 is
`3a8bbab5196d9233434abf33552cd7fc5637c74f0f76cea66cf247eddb8e7287`.

`fp128_dense_bounded_nv20_p1.aks` uses:

```text
--final-group fp128_dense_bounded:20:1
```

It admits exactly one final-only, single-column commitment for the one-MiB
cartridge ROM. The source file has one repository-normalized trailing newline;
the embedded canonical artifact excludes that newline and has SHA-256
`141f4ffaf9b1546351a35582352a6337b2855aab3d77cd456bfe30241d4c40af`.

`fp128_dense_bounded_nv17_p1.aks` uses:

```text
--final-group fp128_dense_bounded:17:1
```

It admits one final-only column for a 128-KiB mutable-memory value,
timestamp, or inverse limb. The source file has one repository-normalized
trailing newline; the embedded canonical artifact excludes that newline and
has SHA-256
`8292b771966f1e30f7919796f5ebad3b6327686b7be61a4c778cd4972719ef15`.

`fp128_dense_bounded_nv17_p128.aks` replaces the final argument with:

```text
--final-group fp128_dense_bounded:17:128
```

It admits one 128-column group for four fixed-capacity segment protocol-log
tables and their inverse witnesses. The source file has one repository-normalized
trailing newline; the embedded canonical artifact excludes that newline and has
SHA-256
`2dba5b6d53ca57eaee58c872ceba3cdf6c7dbfe522162144577e71cadf543a80`.
